[pytest]
# Tests import exactly the way the app does — "from src.core.models import Shot".
# Without this, every test file needs its own sys.path hack, and tests can pass
# on an import path the real program never uses.
pythonpath = .
testpaths = tests
